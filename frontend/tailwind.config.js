/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Must be 'class'
  content: [
    './src/**/*.{html,ts}', // Ensure it scans all your templates
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
